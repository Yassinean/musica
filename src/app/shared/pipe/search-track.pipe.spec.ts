import { SearchTrackPipe } from './search-track.pipe';

describe('SearchTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
